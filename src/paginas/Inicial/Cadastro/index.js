/* async cadastrar() {
    await fb.auth().createUserWithEmailAndPassword(this.state.usuario, this.state.senha)
      .then(async (value) => {

      })
      .catch((error) => {
        if (error.code === "auth/email-already-in-use") {
          alert("Email já está sendo usado!")
        }
      });
  } */